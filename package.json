{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "A simple blog application with React frontend and Node backend",
  "scripts": {
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\"",
    "install-all": "concurrently \"cd frontend && npm install\" \"cd backend && npm install\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 